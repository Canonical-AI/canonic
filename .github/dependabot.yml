version: 2
updates:
  # Keep the SHA-pinned GitHub Actions current via reviewed PRs (so pinning
  # doesn't mean silently running stale, unpatched action versions).
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
